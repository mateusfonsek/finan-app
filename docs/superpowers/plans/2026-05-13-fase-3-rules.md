# Fase 3 — Regras automáticas Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Permitir o usuário criar regras "se a descrição contém X então categoria Y" que são aplicadas automaticamente durante o import de OFX, e oferecer um botão "Aplicar a existentes" pra rodar as regras nas transações já importadas que ainda não têm categoria. Categorização manual NUNCA é sobrescrita.

**Architecture:** Nova tabela `rules (pattern, category_id, priority, created_at)` via migration 0002. Engine de aplicação é uma única UPDATE SQL que casa `LOWER(description) LIKE '%pattern%'` ordenado por `priority DESC, created_at DESC` — first match wins. Aplicação invocada (1) automaticamente no final de `insert_transactions` pro account afetado, (2) explicitamente via `apply_rules_to_uncategorized(account_id?)`. UI minimalista: form inline em `/rules` (sem modal) + tabela com delete; sidebar ganha link "Regras".

**Tech Stack:** já instalado. Nada novo.

**Acceptance criteria (Fase 3):**
1. Migration 0002 cria tabela `rules` + index por priority. Idempotente.
2. CRUD de regras via commands tipados.
3. Criar regra `pattern="uber"` → categoria Transporte. Importar OFX com transação "UBER * TRIP 123" → transação fica auto-categorizada como Transporte na lista.
4. `apply_rules_to_uncategorized()` re-aplica regras em transações com `category_id IS NULL` e retorna quantas foram categorizadas. Nunca sobrescreve manual.
5. `InsertResult` ganha campo `auto_categorized: u32` que reporta quantas das recém-inseridas foram auto-marcadas.
6. UI `/rules`: form inline (pattern + categoria + priority) → criar; lista mostra todas as regras com botão delete; botão "Aplicar regras a transações sem categoria" no header.
7. Sidebar: novo link "Regras" sob seção "Organizar".
8. Tests: `cargo test --lib` ≥ 26 (21 anteriores + 5 novos), `pnpm test` ≥ 19, `pnpm check` 0, clippy/fmt limpos.

**Out of scope:**
- Regex (apenas substring case-insensitive)
- Bank-specific rule scoping
- "Aplicar a esta regra apenas" (rerun por linha)
- Drag-to-reorder priority (apenas number input)
- Bulk import/export de regras

---

## Estrutura de arquivos

```
src-tauri/
└── src/
    ├── migrations/
    │   └── 0002_rules.sql               T1 (novo)
    ├── db/
    │   └── migrations.rs                T1 (adicionar ao MIGRATIONS const)
    ├── domain/
    │   ├── rule.rs                      T1 (novo)
    │   └── mod.rs                       T1 (export rule)
    ├── domain/
    │   └── transaction.rs               T2 (InsertResult ganha auto_categorized)
    ├── commands/
    │   ├── rules.rs                     T2 (novo)
    │   ├── transactions.rs              T3 (auto-apply no insert_transactions)
    │   └── mod.rs                       T2 (declarar rules)
    └── lib.rs                           T4 (registrar 5 commands novos)

src/
└── lib/
    ├── api/
    │   └── rules.ts                     T5 (novo)
    └── components/
        ├── shell/Sidebar.svelte         T7 (adicionar link Regras)
        └── rules/
            ├── RuleForm.svelte          T6
            └── RulesList.svelte         T6
└── routes/
    ├── routes.ts                        T7 (adicionar /rules)
    └── Rules.svelte                     T6 (rewrite — atualmente stub não existe)
```

> **Nota sobre routes.ts:** o arquivo atual mapeia 7 paths (`/`, `/onboarding`, `/dashboard`, `/transactions`, `/import`, `/categories`, `/settings`). Vamos adicionar `/rules` (a tela ainda não existe — não foi criada na F0 porque foi marcada como "fase 3"; vamos criar agora).

---

## Task 1: Migration 0002 + domain::rule

**Files:**
- Create: `src-tauri/migrations/0002_rules.sql`
- Create: `src-tauri/src/domain/rule.rs`
- Modify: `src-tauri/src/domain/mod.rs`
- Modify: `src-tauri/src/db/migrations.rs`

- [ ] **Step 1: Create `src-tauri/migrations/0002_rules.sql`**

```sql
CREATE TABLE rules (
  id INTEGER PRIMARY KEY,
  pattern TEXT NOT NULL,
  category_id INTEGER NOT NULL REFERENCES categories(id),
  priority INTEGER NOT NULL DEFAULT 0,
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
);

CREATE INDEX idx_rules_priority ON rules(priority DESC, created_at DESC);
```

- [ ] **Step 2: Create `src-tauri/src/domain/rule.rs`**

```rust
use serde::{Deserialize, Serialize};
use specta::Type;

#[derive(Debug, Clone, Serialize, Deserialize, Type)]
pub struct Rule {
    pub id: i64,
    pub pattern: String,
    pub category_id: i64,
    pub priority: i32,
    pub created_at: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, Type)]
pub struct NewRule {
    pub pattern: String,
    pub category_id: i64,
    pub priority: i32,
}

#[derive(Debug, Clone, Serialize, Deserialize, Type)]
pub struct UpdateRule {
    pub pattern: String,
    pub category_id: i64,
    pub priority: i32,
}
```

- [ ] **Step 3: Update `src-tauri/src/domain/mod.rs`**

```rust
pub mod account;
pub mod category;
pub mod rule;
pub mod transaction;
```

- [ ] **Step 4: Update `src-tauri/src/db/migrations.rs`**

Find the `MIGRATIONS` const (currently one entry for `0001_init`) and add the new migration:

```rust
const MIGRATIONS: &[(&str, &str)] = &[
    ("0001_init", include_str!("../../migrations/0001_init.sql")),
    ("0002_rules", include_str!("../../migrations/0002_rules.sql")),
];
```

Add one test to the existing `mod tests` block (after the existing ones):

```rust
    #[test]
    fn applies_rules_migration() {
        let conn = Connection::open_in_memory().unwrap();
        apply(&conn).unwrap();

        assert!(table_exists(&conn, "rules"));
        let applied: Vec<String> = {
            let mut stmt = conn
                .prepare("SELECT name FROM _migrations ORDER BY name")
                .unwrap();
            stmt.query_map([], |r| r.get::<_, String>(0))
                .unwrap()
                .map(|r| r.unwrap())
                .collect()
        };
        assert_eq!(applied, vec!["0001_init".to_string(), "0002_rules".to_string()]);
    }
```

- [ ] **Step 5: Run cargo test**

```bash
cd src-tauri && cargo test --lib 2>&1 | tail -20 && cd ..
```
Expected: **22 tests pass** (21 prior + 1 new). `applies_rules_migration` is in `db::migrations::tests`.

> **Atenção:** se algum teste existente quebrar com "table rules already exists" ou similar, é porque o teste reusa um arquivo SQLite real (improvável — usamos `:memory:`). Investigue. O esperado é apenas o teste novo passar.

- [ ] **Step 6: Commit**

```bash
git add src-tauri/migrations/0002_rules.sql src-tauri/src/domain/ src-tauri/src/db/migrations.rs
git commit -m "$(cat <<'EOF'
feat(db): migration 0002 + Rule/NewRule/UpdateRule domain

- rules table: pattern, category_id FK, priority INT, created_at
- index priority DESC, created_at DESC (ordem de match)
- domain/rule.rs com 3 structs serde + specta
- migrations.rs aplica 0002 no startup; teste valida _migrations tracking

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: Rules CRUD commands + tests

**Files:**
- Create: `src-tauri/src/commands/rules.rs`
- Modify: `src-tauri/src/commands/mod.rs`

- [ ] **Step 1: Update `src-tauri/src/commands/mod.rs`**

```rust
pub mod accounts;
pub mod categories;
pub mod health;
pub mod rules;
pub mod transactions;
```

- [ ] **Step 2: Create `src-tauri/src/commands/rules.rs`**

```rust
use rusqlite::params;
use tauri::State;

use crate::db::Db;
use crate::domain::rule::{NewRule, Rule, UpdateRule};
use crate::error::{AppError, AppResult};

#[tauri::command]
#[specta::specta]
pub fn list_rules(db: State<'_, Db>) -> AppResult<Vec<Rule>> {
    let conn = db.conn.lock().expect("db mutex poisoned");
    let mut stmt = conn.prepare(
        "SELECT id, pattern, category_id, priority, created_at
         FROM rules
         ORDER BY priority DESC, created_at DESC",
    )?;
    let rows = stmt.query_map([], |row| {
        Ok(Rule {
            id: row.get(0)?,
            pattern: row.get(1)?,
            category_id: row.get(2)?,
            priority: row.get(3)?,
            created_at: row.get(4)?,
        })
    })?;
    rows.collect::<rusqlite::Result<Vec<_>>>().map_err(AppError::from)
}

#[tauri::command]
#[specta::specta]
pub fn create_rule(db: State<'_, Db>, input: NewRule) -> AppResult<Rule> {
    if input.pattern.trim().is_empty() {
        return Err(AppError::Invalid("pattern must not be empty".into()));
    }
    let conn = db.conn.lock().expect("db mutex poisoned");
    conn.execute(
        "INSERT INTO rules (pattern, category_id, priority) VALUES (?1, ?2, ?3)",
        params![input.pattern.trim(), input.category_id, input.priority],
    )?;
    let id = conn.last_insert_rowid();
    fetch_rule(&conn, id)
}

#[tauri::command]
#[specta::specta]
pub fn update_rule(db: State<'_, Db>, rule_id: i64, input: UpdateRule) -> AppResult<Rule> {
    if input.pattern.trim().is_empty() {
        return Err(AppError::Invalid("pattern must not be empty".into()));
    }
    let conn = db.conn.lock().expect("db mutex poisoned");
    let changed = conn.execute(
        "UPDATE rules SET pattern = ?1, category_id = ?2, priority = ?3 WHERE id = ?4",
        params![
            input.pattern.trim(),
            input.category_id,
            input.priority,
            rule_id
        ],
    )?;
    if changed == 0 {
        return Err(AppError::Invalid(format!("rule {rule_id} not found")));
    }
    fetch_rule(&conn, rule_id)
}

#[tauri::command]
#[specta::specta]
pub fn delete_rule(db: State<'_, Db>, rule_id: i64) -> AppResult<()> {
    let conn = db.conn.lock().expect("db mutex poisoned");
    let changed = conn.execute("DELETE FROM rules WHERE id = ?1", params![rule_id])?;
    if changed == 0 {
        return Err(AppError::Invalid(format!("rule {rule_id} not found")));
    }
    Ok(())
}

/// Run all rules on transactions with `category_id IS NULL` (manual categorization
/// is never overwritten). When multiple rules match the same transaction, the one
/// with the highest priority wins; ties broken by most recent created_at.
/// Returns the count of transactions newly categorized.
#[tauri::command]
#[specta::specta]
pub fn apply_rules_to_uncategorized(
    db: State<'_, Db>,
    account_id: Option<i64>,
) -> AppResult<u32> {
    let mut conn = db.conn.lock().expect("db mutex poisoned");
    apply_rules_internal(&mut conn, account_id)
}

/// Shared engine used both by the public command and by insert_transactions
/// (post-batch auto-categorize). Single UPDATE that picks the highest-priority
/// matching rule per row.
pub fn apply_rules_internal(
    conn: &mut rusqlite::Connection,
    account_id: Option<i64>,
) -> AppResult<u32> {
    let scope_filter = match account_id {
        Some(_) => "AND account_id = ?1",
        None => "",
    };
    let sql = format!(
        "UPDATE transactions
         SET category_id = (
             SELECT r.category_id FROM rules r
             WHERE LOWER(transactions.description) LIKE '%' || LOWER(r.pattern) || '%'
             ORDER BY r.priority DESC, r.created_at DESC
             LIMIT 1
         )
         WHERE category_id IS NULL
           AND EXISTS (
               SELECT 1 FROM rules r
               WHERE LOWER(transactions.description) LIKE '%' || LOWER(r.pattern) || '%'
           )
           {scope_filter}",
    );
    let changed = if let Some(id) = account_id {
        conn.execute(&sql, params![id])?
    } else {
        conn.execute(&sql, [])?
    };
    Ok(changed as u32)
}

fn fetch_rule(conn: &rusqlite::Connection, id: i64) -> AppResult<Rule> {
    conn.query_row(
        "SELECT id, pattern, category_id, priority, created_at FROM rules WHERE id = ?1",
        params![id],
        |row| {
            Ok(Rule {
                id: row.get(0)?,
                pattern: row.get(1)?,
                category_id: row.get(2)?,
                priority: row.get(3)?,
                created_at: row.get(4)?,
            })
        },
    )
    .map_err(AppError::from)
}

#[cfg(test)]
mod tests {
    use super::apply_rules_internal;
    use crate::db::migrations;
    use rusqlite::{params, Connection};

    fn fresh_conn() -> Connection {
        let conn = Connection::open_in_memory().unwrap();
        migrations::apply(&conn).unwrap();
        conn
    }

    fn category_id(conn: &Connection, name: &str) -> i64 {
        conn.query_row(
            "SELECT id FROM categories WHERE name = ?1",
            params![name],
            |r| r.get(0),
        )
        .unwrap()
    }

    fn insert_account(conn: &Connection) -> i64 {
        conn.execute(
            "INSERT INTO accounts (name, bank, ofx_acctid) VALUES ('test', NULL, 'A1')",
            [],
        )
        .unwrap();
        conn.last_insert_rowid()
    }

    fn insert_tx(conn: &Connection, account_id: i64, description: &str, category_id: Option<i64>) -> i64 {
        conn.execute(
            "INSERT INTO transactions (account_id, date, amount, description, category_id, ofx_fitid)
             VALUES (?1, '2026-04-12', '10.00', ?2, ?3, NULL)",
            params![account_id, description, category_id],
        )
        .unwrap();
        conn.last_insert_rowid()
    }

    fn insert_rule(conn: &Connection, pattern: &str, cat: i64, priority: i32) -> i64 {
        conn.execute(
            "INSERT INTO rules (pattern, category_id, priority) VALUES (?1, ?2, ?3)",
            params![pattern, cat, priority],
        )
        .unwrap();
        conn.last_insert_rowid()
    }

    #[test]
    fn applies_rule_case_insensitive() {
        let mut conn = fresh_conn();
        let acc = insert_account(&conn);
        let transporte = category_id(&conn, "Transporte");
        insert_rule(&conn, "uber", transporte, 0);
        let tx_id = insert_tx(&conn, acc, "UBER * TRIP 12345", None);

        let n = apply_rules_internal(&mut conn, None).unwrap();
        assert_eq!(n, 1);

        let cat: Option<i64> = conn
            .query_row(
                "SELECT category_id FROM transactions WHERE id = ?1",
                params![tx_id],
                |r| r.get(0),
            )
            .unwrap();
        assert_eq!(cat, Some(transporte));
    }

    #[test]
    fn higher_priority_rule_wins() {
        let mut conn = fresh_conn();
        let acc = insert_account(&conn);
        let transporte = category_id(&conn, "Transporte");
        let outros = category_id(&conn, "Outros");

        insert_rule(&conn, "uber", outros, 0);
        insert_rule(&conn, "uber trip", transporte, 10);

        let tx_id = insert_tx(&conn, acc, "uber trip via app", None);
        apply_rules_internal(&mut conn, None).unwrap();

        let cat: Option<i64> = conn
            .query_row(
                "SELECT category_id FROM transactions WHERE id = ?1",
                params![tx_id],
                |r| r.get(0),
            )
            .unwrap();
        assert_eq!(cat, Some(transporte), "priority 10 wins over priority 0");
    }

    #[test]
    fn manual_categorization_is_never_overwritten() {
        let mut conn = fresh_conn();
        let acc = insert_account(&conn);
        let mercado = category_id(&conn, "Mercado");
        let transporte = category_id(&conn, "Transporte");

        insert_rule(&conn, "uber", transporte, 0);
        let tx_id = insert_tx(&conn, acc, "UBER trip", Some(mercado));

        let n = apply_rules_internal(&mut conn, None).unwrap();
        assert_eq!(n, 0, "manual category preserved");

        let cat: Option<i64> = conn
            .query_row(
                "SELECT category_id FROM transactions WHERE id = ?1",
                params![tx_id],
                |r| r.get(0),
            )
            .unwrap();
        assert_eq!(cat, Some(mercado));
    }

    #[test]
    fn scope_to_account_id() {
        let mut conn = fresh_conn();
        conn.execute(
            "INSERT INTO accounts (name, bank, ofx_acctid) VALUES ('a1', NULL, 'A1'),
                                                                  ('a2', NULL, 'A2')",
            [],
        )
        .unwrap();
        let a1: i64 = conn
            .query_row("SELECT id FROM accounts WHERE ofx_acctid='A1'", [], |r| r.get(0))
            .unwrap();
        let a2: i64 = conn
            .query_row("SELECT id FROM accounts WHERE ofx_acctid='A2'", [], |r| r.get(0))
            .unwrap();
        let transporte = category_id(&conn, "Transporte");
        insert_rule(&conn, "uber", transporte, 0);
        insert_tx(&conn, a1, "uber a1", None);
        insert_tx(&conn, a2, "uber a2", None);

        let n = apply_rules_internal(&mut conn, Some(a1)).unwrap();
        assert_eq!(n, 1, "scope must limit to account a1");

        let a1_cat: Option<i64> = conn
            .query_row(
                "SELECT category_id FROM transactions WHERE account_id = ?1",
                params![a1],
                |r| r.get(0),
            )
            .unwrap();
        let a2_cat: Option<i64> = conn
            .query_row(
                "SELECT category_id FROM transactions WHERE account_id = ?1",
                params![a2],
                |r| r.get(0),
            )
            .unwrap();
        assert_eq!(a1_cat, Some(transporte));
        assert_eq!(a2_cat, None);
    }

    #[test]
    fn pattern_with_no_match_does_nothing() {
        let mut conn = fresh_conn();
        let acc = insert_account(&conn);
        let transporte = category_id(&conn, "Transporte");
        insert_rule(&conn, "uber", transporte, 0);
        insert_tx(&conn, acc, "padaria do bairro", None);

        let n = apply_rules_internal(&mut conn, None).unwrap();
        assert_eq!(n, 0);
    }
}
```

- [ ] **Step 3: Run cargo test**

```bash
cd src-tauri && cargo test --lib 2>&1 | tail -20 && cd ..
```
Expected: **27 tests pass** (22 prior + 5 new from `rules::tests`).

- [ ] **Step 4: Commit**

```bash
git add src-tauri/src/commands/
git commit -m "$(cat <<'EOF'
feat(commands): rules CRUD + apply_rules_to_uncategorized engine

- list_rules, create_rule, update_rule, delete_rule
- apply_rules_to_uncategorized(account_id?): retorna count atualizado
- apply_rules_internal: única UPDATE SQL com correlated subquery
  ORDER BY priority DESC, created_at DESC LIMIT 1 (first match wins)
- Validação: pattern não vazio
- 5 testes (case-insensitive, priority wins, manual preserved, account scope, no match)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: Integrate auto-apply in insert_transactions + InsertResult.auto_categorized

**Files:**
- Modify: `src-tauri/src/domain/transaction.rs` (extend InsertResult)
- Modify: `src-tauri/src/commands/transactions.rs` (call apply_rules_internal at end of batch)

- [ ] **Step 1: Extend `InsertResult` in `src-tauri/src/domain/transaction.rs`**

Find:
```rust
#[derive(Debug, Clone, Serialize, Deserialize, Type)]
pub struct InsertResult {
    pub inserted: u32,
    pub skipped_duplicates: u32,
}
```

Replace with:
```rust
#[derive(Debug, Clone, Serialize, Deserialize, Type)]
pub struct InsertResult {
    pub inserted: u32,
    pub skipped_duplicates: u32,
    pub auto_categorized: u32,
}
```

- [ ] **Step 2: Modify `insert_transactions` in `src-tauri/src/commands/transactions.rs`**

After the inner `tx_conn.commit()?` and BEFORE returning `Ok(InsertResult{...})`, run rules:

Find the existing block (last few lines of `insert_transactions`):
```rust
    tx_conn.commit()?;

    Ok(InsertResult {
        inserted,
        skipped_duplicates: skipped,
    })
}
```

Replace with:
```rust
    tx_conn.commit()?;

    let auto_categorized = crate::commands::rules::apply_rules_internal(&mut conn, Some(account_id))?;

    Ok(InsertResult {
        inserted,
        skipped_duplicates: skipped,
        auto_categorized,
    })
}
```

> **Nota:** `apply_rules_internal` recebe `&mut Connection` mas o lock está em `conn`. Como `conn` é `MutexGuard<Connection>`, podemos passar `&mut *conn` ou simplesmente `&mut conn` se a função aceita. Verifique se compila; se houver erro de lifetime, ajuste pra `&mut *conn`. A função foi declarada `pub fn apply_rules_internal(conn: &mut rusqlite::Connection, ...)` — devemos passar `&mut *conn`.

Correção: substitua a linha por:
```rust
    let auto_categorized = crate::commands::rules::apply_rules_internal(&mut *conn, Some(account_id))?;
```

- [ ] **Step 3: Run cargo test**

```bash
cd src-tauri && cargo test --lib 2>&1 | tail -20 && cd ..
```
Expected: **27 tests pass**. The previously-passing `insert_batch_inserts_all_when_no_dups` etc. still pass — they use a raw helper (not the command) so the struct field change doesn't break them.

If a test fails because the struct destructuring is incomplete, find the test and update.

- [ ] **Step 4: Commit**

```bash
git add src-tauri/src/domain/transaction.rs src-tauri/src/commands/transactions.rs
git commit -m "$(cat <<'EOF'
feat(import): auto-aplicar regras no fim de insert_transactions

- InsertResult ganha auto_categorized: u32
- insert_transactions chama apply_rules_internal(account_id) após commit do batch
- TS bindings reportarão o novo campo automaticamente em T4

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: Register new commands + regenerate bindings

**Files:**
- Modify: `src-tauri/src/lib.rs`

- [ ] **Step 1: Edit collect_commands!**

Append the 5 new rules commands to the list:

```rust
let specta_builder = Builder::<tauri::Wry>::new().commands(collect_commands![
    commands::health::health_check,
    commands::accounts::list_accounts,
    commands::accounts::create_or_get_account,
    commands::categories::list_categories,
    commands::categories::create_category,
    commands::transactions::list_transactions,
    commands::transactions::insert_transactions,
    commands::transactions::check_existing_fitids,
    commands::transactions::update_transaction_category,
    commands::transactions::update_transaction_notes,
    commands::rules::list_rules,
    commands::rules::create_rule,
    commands::rules::update_rule,
    commands::rules::delete_rule,
    commands::rules::apply_rules_to_uncategorized,
]);
```

Everything else (`@ts-nocheck` block, `BigIntExportBehavior::Number`, setup) stays the same.

- [ ] **Step 2: Regenerate bindings**

```bash
pkill -f "tauri dev" 2>/dev/null; pkill -f "vite" 2>/dev/null; pkill -f "target/debug/finan" 2>/dev/null
pnpm tauri dev > /tmp/finan-dev.log 2>&1 &
until grep -q "applyRulesToUncategorized" src/lib/bindings.ts 2>/dev/null && \
      grep -q "Rule" src/lib/bindings.ts 2>/dev/null; do
  sleep 3
done
pkill -f "tauri dev"; pkill -f "vite"; pkill -f "target/debug/finan"
sleep 2
grep "export type" src/lib/bindings.ts
echo "---"
grep "async " src/lib/bindings.ts
```

Expected new types: `Rule`, `NewRule`, `UpdateRule`. `InsertResult` should now show `auto_categorized: number`.
Expected new async functions: `listRules`, `createRule`, `updateRule`, `deleteRule`, `applyRulesToUncategorized`.

- [ ] **Step 3: pnpm check**

```bash
pnpm check 2>&1 | tail -5
```
Expected: 0 errors.

- [ ] **Step 4: Commit**

```bash
git add src-tauri/src/lib.rs
git commit -m "$(cat <<'EOF'
feat(ipc): registra commands de rules + auto_categorized no specta builder

- 15 commands totais
- bindings.ts regenerado: Rule, NewRule, UpdateRule + 5 funções async
- InsertResult.auto_categorized aparece automaticamente

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: TS API wrappers for rules

**Files:**
- Create: `src/lib/api/rules.ts`

- [ ] **Step 1: Create `src/lib/api/rules.ts`**

```ts
import { commands } from "../bindings";
import type { NewRule, Rule, UpdateRule } from "../bindings";

function unwrap<T>(result: { status: "ok"; data: T } | { status: "error"; error: string }): T {
  if (result.status === "error") throw new Error(result.error);
  return result.data;
}

export async function listRules(): Promise<Rule[]> {
  return unwrap(await commands.listRules());
}

export async function createRule(input: NewRule): Promise<Rule> {
  return unwrap(await commands.createRule(input));
}

export async function updateRule(ruleId: number, input: UpdateRule): Promise<Rule> {
  return unwrap(await commands.updateRule(ruleId, input));
}

export async function deleteRule(ruleId: number): Promise<void> {
  await commands.deleteRule(ruleId).then((r) => {
    if (r.status === "error") throw new Error(r.error);
  });
}

export async function applyRulesToUncategorized(
  accountId: number | null = null,
): Promise<number> {
  return unwrap(await commands.applyRulesToUncategorized(accountId));
}
```

- [ ] **Step 2: pnpm check**

```bash
pnpm check 2>&1 | tail -5
```
Expected: 0 errors.

- [ ] **Step 3: Commit**

```bash
git add src/lib/api/rules.ts
git commit -m "$(cat <<'EOF'
feat(api): wrappers TS pra rules (list/create/update/delete/apply)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: Rules UI — RuleForm + RulesList + Rules.svelte route

**Files:**
- Create: `src/lib/components/rules/RuleForm.svelte`
- Create: `src/lib/components/rules/RulesList.svelte`
- Create: `src/routes/Rules.svelte`

- [ ] **Step 1: Create `src/lib/components/rules/RuleForm.svelte`**

Form inline pra criar OU editar uma regra. Em modo "editar", chamamos com `initial` preenchido; o pai gerencia se é new ou update.

```svelte
<script lang="ts">
  import { Button } from "$lib/components/ui/button";
  import type { Category, Rule } from "$lib/bindings";

  type Props = {
    categories: Category[];
    initial?: Rule | null;
    onSave: (data: { pattern: string; categoryId: number; priority: number }) => Promise<void>;
    onCancel?: () => void;
    submitLabel?: string;
  };

  let { categories, initial = null, onSave, onCancel, submitLabel }: Props = $props();

  let pattern = $state(initial?.pattern ?? "");
  let categoryId = $state<number | null>(initial?.category_id ?? null);
  let priority = $state(initial?.priority ?? 0);
  let busy = $state(false);
  let error = $state<string | null>(null);

  $effect(() => {
    pattern = initial?.pattern ?? "";
    categoryId = initial?.category_id ?? null;
    priority = initial?.priority ?? 0;
  });

  async function submit(e: Event) {
    e.preventDefault();
    error = null;
    if (pattern.trim().length === 0) {
      error = "Pattern não pode ser vazio.";
      return;
    }
    if (categoryId === null) {
      error = "Selecione uma categoria.";
      return;
    }
    busy = true;
    try {
      await onSave({ pattern: pattern.trim(), categoryId, priority });
      if (!initial) {
        pattern = "";
        categoryId = null;
        priority = 0;
      }
    } catch (e) {
      error = e instanceof Error ? e.message : String(e);
    } finally {
      busy = false;
    }
  }
</script>

<form onsubmit={submit} class="rounded-lg border border-border-subtle bg-surface p-4 flex flex-col gap-3">
  <div class="text-[10.5px] uppercase tracking-wider font-semibold text-fg-faint">
    {initial ? "Editar regra" : "Nova regra"}
  </div>

  <div class="grid grid-cols-[1fr_180px_100px_auto] gap-2 items-end">
    <label class="flex flex-col gap-1">
      <span class="text-[11px] text-fg-muted">Pattern (descrição contém)</span>
      <input
        bind:value={pattern}
        placeholder="ex: uber"
        class="rounded-md border border-border bg-surface-2 px-2 py-1 text-[12px] text-fg focus:outline-none focus:border-accent"
      />
    </label>

    <label class="flex flex-col gap-1">
      <span class="text-[11px] text-fg-muted">Categoria</span>
      <select
        value={categoryId === null ? "" : String(categoryId)}
        onchange={(e) => {
          const v = (e.currentTarget as HTMLSelectElement).value;
          categoryId = v === "" ? null : Number(v);
        }}
        class="rounded-md border border-border bg-surface-2 px-2 py-1 text-[12px] text-fg"
      >
        <option value="">— selecione —</option>
        {#each categories as c}
          <option value={String(c.id)}>{c.name}</option>
        {/each}
      </select>
    </label>

    <label class="flex flex-col gap-1">
      <span class="text-[11px] text-fg-muted">Prioridade</span>
      <input
        type="number"
        bind:value={priority}
        class="rounded-md border border-border bg-surface-2 px-2 py-1 text-[12px] text-fg tabular focus:outline-none focus:border-accent"
      />
    </label>

    <div class="flex gap-2">
      {#if onCancel}
        <Button variant="ghost" onclick={onCancel} type="button">Cancelar</Button>
      {/if}
      <Button type="submit" disabled={busy}>
        {busy ? "Salvando…" : (submitLabel ?? (initial ? "Salvar" : "Adicionar"))}
      </Button>
    </div>
  </div>

  {#if error}
    <div class="text-[11px] text-neg">{error}</div>
  {/if}
</form>
```

- [ ] **Step 2: Create `src/lib/components/rules/RulesList.svelte`**

```svelte
<script lang="ts">
  import type { Category, Rule } from "$lib/bindings";

  type Props = {
    rules: Rule[];
    categories: Category[];
    onEdit: (rule: Rule) => void;
    onDelete: (rule: Rule) => Promise<void>;
  };

  let { rules, categories, onEdit, onDelete }: Props = $props();

  function categoryName(id: number): string {
    return categories.find((c) => c.id === id)?.name ?? "?";
  }
  function categoryToken(id: number): string {
    return categories.find((c) => c.id === id)?.color_token ?? "--color-cat-outros";
  }
</script>

<div class="rounded-lg border border-border-subtle bg-surface overflow-hidden">
  <table class="w-full text-[12px]">
    <thead class="bg-surface-2">
      <tr>
        <th class="text-left px-4 py-2 font-medium text-fg-faint uppercase tracking-wider text-[10.5px]">Pattern</th>
        <th class="text-left px-4 py-2 font-medium text-fg-faint uppercase tracking-wider text-[10.5px] w-[180px]">Categoria</th>
        <th class="text-right px-4 py-2 font-medium text-fg-faint uppercase tracking-wider text-[10.5px] w-[100px]">Prioridade</th>
        <th class="px-4 py-2 w-[120px]"></th>
      </tr>
    </thead>
    <tbody>
      {#each rules as r (r.id)}
        <tr class="border-t border-border-subtle hover:bg-hover">
          <td class="px-4 py-2.5 font-mono text-[11.5px]">{r.pattern}</td>
          <td class="px-4 py-2.5">
            <span class="inline-flex items-center gap-1.5">
              <span class="w-2 h-2 rounded-full" style="background: var({categoryToken(r.category_id)})"></span>
              {categoryName(r.category_id)}
            </span>
          </td>
          <td class="px-4 py-2.5 text-right tabular">{r.priority}</td>
          <td class="px-4 py-2.5 text-right flex gap-2 justify-end">
            <button
              type="button"
              onclick={() => onEdit(r)}
              class="text-[11px] text-fg-muted hover:text-fg underline-offset-2 hover:underline"
            >
              Editar
            </button>
            <button
              type="button"
              onclick={() => onDelete(r)}
              class="text-[11px] text-neg hover:underline underline-offset-2"
            >
              Apagar
            </button>
          </td>
        </tr>
      {:else}
        <tr>
          <td colspan="4" class="px-4 py-10 text-center text-fg-faint">
            Nenhuma regra ainda. Crie uma acima — ex. pattern "uber" → Transporte.
          </td>
        </tr>
      {/each}
    </tbody>
  </table>
</div>
```

- [ ] **Step 3: Create `src/routes/Rules.svelte`**

```svelte
<script lang="ts">
  import { onMount } from "svelte";
  import { Button } from "$lib/components/ui/button";
  import RuleForm from "$lib/components/rules/RuleForm.svelte";
  import RulesList from "$lib/components/rules/RulesList.svelte";
  import { listCategories } from "$lib/api/categories";
  import {
    listRules,
    createRule,
    updateRule,
    deleteRule,
    applyRulesToUncategorized,
  } from "$lib/api/rules";
  import type { Category, Rule } from "$lib/bindings";

  let rules = $state<Rule[]>([]);
  let categories = $state<Category[]>([]);
  let editing = $state<Rule | null>(null);
  let loading = $state(true);
  let error = $state<string | null>(null);
  let applyMsg = $state<string | null>(null);
  let applying = $state(false);

  async function refresh() {
    rules = await listRules();
  }

  onMount(async () => {
    try {
      [categories, rules] = await Promise.all([listCategories(), listRules()]);
    } catch (e) {
      error = e instanceof Error ? e.message : String(e);
    } finally {
      loading = false;
    }
  });

  async function onCreate(data: { pattern: string; categoryId: number; priority: number }) {
    await createRule({ pattern: data.pattern, category_id: data.categoryId, priority: data.priority });
    await refresh();
  }

  async function onUpdate(data: { pattern: string; categoryId: number; priority: number }) {
    if (!editing) return;
    await updateRule(editing.id, {
      pattern: data.pattern,
      category_id: data.categoryId,
      priority: data.priority,
    });
    editing = null;
    await refresh();
  }

  async function onDelete(rule: Rule) {
    const ok = confirm(`Apagar regra "${rule.pattern}"?`);
    if (!ok) return;
    await deleteRule(rule.id);
    await refresh();
  }

  async function onApply() {
    applying = true;
    applyMsg = null;
    try {
      const n = await applyRulesToUncategorized(null);
      applyMsg = n === 0
        ? "Nenhuma transação foi categorizada (nenhum pattern bateu)."
        : `${n} ${n === 1 ? "transação" : "transações"} categorizada${n === 1 ? "" : "s"} automaticamente.`;
    } catch (e) {
      error = e instanceof Error ? e.message : String(e);
    } finally {
      applying = false;
    }
  }
</script>

<section class="p-8 max-w-5xl mx-auto flex flex-col gap-5">
  <header class="flex items-baseline justify-between gap-4 flex-wrap">
    <div class="flex flex-col gap-1">
      <h2 class="text-xl font-semibold tracking-tight" style="font-family: var(--font-display)">
        Regras
      </h2>
      <p class="text-xs text-fg-faint max-w-xl">
        Regras "se descrição contém X, categoria Y" são aplicadas automaticamente em cada import.
        Categorização manual nunca é sobrescrita.
      </p>
    </div>
    <div class="flex items-center gap-2">
      {#if applyMsg}
        <span class="text-[11px] text-fg-faint">{applyMsg}</span>
      {/if}
      <Button onclick={onApply} disabled={applying || rules.length === 0}>
        {applying ? "Aplicando…" : "Aplicar a existentes"}
      </Button>
    </div>
  </header>

  {#if loading}
    <div class="text-fg-faint text-sm">Carregando…</div>
  {:else}
    {#if error}
      <div class="rounded-lg border border-border bg-surface p-3 text-sm text-neg">{error}</div>
    {/if}

    {#if editing}
      <RuleForm
        {categories}
        initial={editing}
        onSave={onUpdate}
        onCancel={() => (editing = null)}
        submitLabel="Salvar alterações"
      />
    {:else}
      <RuleForm {categories} onSave={onCreate} />
    {/if}

    <RulesList
      {rules}
      {categories}
      onEdit={(r) => (editing = r)}
      {onDelete}
    />
  {/if}
</section>
```

- [ ] **Step 4: pnpm check**

```bash
pnpm check 2>&1 | tail -5
```
Expected: 0 errors.

- [ ] **Step 5: Commit**

```bash
git add src/lib/components/rules/ src/routes/Rules.svelte
git commit -m "$(cat <<'EOF'
feat(rules): UI completa — RuleForm + RulesList + /rules route

- Form inline (pattern + categoria + prioridade) com validação client-side
- Mesma RuleForm reusada em modo "Editar" via prop initial
- RulesList tabela com swatch de cor + editar/apagar
- Botão "Aplicar a existentes" no header dispara apply_rules_to_uncategorized
- confirm() nativo no delete

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: Sidebar adiciona link "Regras" + routes.ts mapping

**Files:**
- Modify: `src/routes/routes.ts`
- Modify: `src/lib/components/shell/Sidebar.svelte`

- [ ] **Step 1: Update `src/routes/routes.ts`**

Add the import and route mapping. Replace whole file:

```ts
import Onboarding from "./Onboarding.svelte";
import Dashboard from "./Dashboard.svelte";
import Transactions from "./Transactions.svelte";
import Import from "./Import.svelte";
import Categories from "./Categories.svelte";
import Rules from "./Rules.svelte";
import Settings from "./Settings.svelte";

export const routes = {
  "/": Dashboard,
  "/onboarding": Onboarding,
  "/dashboard": Dashboard,
  "/transactions": Transactions,
  "/import": Import,
  "/categories": Categories,
  "/rules": Rules,
  "/settings": Settings,
};
```

- [ ] **Step 2: Update `src/lib/components/shell/Sidebar.svelte`**

Find the `navItems` array (current values are: Dashboard, Transações, Importar OFX, Categorias). Insert a new entry for Regras under "Organizar":

```ts
const navItems: NavItem[] = [
  { section: "Visão geral", href: "/dashboard", label: "Dashboard" },
  { section: "Visão geral", href: "/transactions", label: "Transações" },
  { section: "Importar", href: "/import", label: "Importar OFX" },
  { section: "Organizar", href: "/categories", label: "Categorias" },
  { section: "Organizar", href: "/rules", label: "Regras" },
];
```

(Don't touch any other lines — section labels and rendering loop are unchanged.)

- [ ] **Step 3: pnpm check**

```bash
pnpm check 2>&1 | tail -5
```
Expected: 0 errors.

- [ ] **Step 4: Smoke test boot**

```bash
pkill -f "tauri dev" 2>/dev/null; pkill -f "vite" 2>/dev/null; pkill -f "target/debug/finan" 2>/dev/null
pnpm tauri dev > /tmp/finan-dev.log 2>&1 &
until lsof -iTCP:1420 -sTCP:LISTEN >/dev/null 2>&1; do sleep 2; done
grep -iE "error|fail" /tmp/finan-dev.log | head -5
pkill -f "tauri dev"; pkill -f "vite"; pkill -f "target/debug/finan"
```
Expected: dev boots clean. DB on first run auto-applies migration 0002 if not already.

Verify rules table exists:
```bash
sqlite3 ~/Library/Application\ Support/app.finan/finan.db ".tables"
```
Should now include `rules` alongside `accounts categories transactions _migrations`.

- [ ] **Step 5: Commit**

```bash
git add src/routes/routes.ts src/lib/components/shell/Sidebar.svelte
git commit -m "$(cat <<'EOF'
feat(nav): link "Regras" na sidebar + rota /rules

- routes.ts mapeia /rules → Rules.svelte
- Sidebar.svelte sob seção "Organizar" depois de "Categorias"

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: Acceptance verification + Fase 3 close

**Files:**
- Modify: `README.md`
- (verificação)

- [ ] **Step 1: Full test suite**

```bash
pnpm check 2>&1 | tail -3
echo "=== pnpm test ==="
pnpm test 2>&1 | tail -5
echo "=== cargo test ==="
cd src-tauri && cargo test --lib 2>&1 | tail -3 && cd ..
echo "=== clippy ==="
cd src-tauri && cargo clippy --all-targets -- -D warnings 2>&1 | tail -5 && cd ..
echo "=== fmt ==="
cd src-tauri && cargo fmt --check 2>&1 | tail -3 && cd ..
echo "=== build ==="
pnpm build 2>&1 | tail -5
```

Expected: pnpm check 0 / pnpm test 19+ / cargo test 27+ / clippy clean / fmt clean / build OK.

If `cargo fmt --check` shows diffs, run `cargo fmt` to apply and re-check.

- [ ] **Step 2: Manual end-to-end test**

```bash
pkill -f "tauri dev" 2>/dev/null
pnpm tauri dev > /tmp/finan-dev.log 2>&1 &
until lsof -iTCP:1420 -sTCP:LISTEN >/dev/null 2>&1; do sleep 2; done
echo "ready"
```

Manual checklist:
- [ ] Sidebar mostra "Regras" sob "Organizar". Click navega pra `/rules`.
- [ ] `/rules` mostra form vazio + tabela "Nenhuma regra ainda".
- [ ] Criar regra: pattern "uber" + categoria Transporte + prioridade 0 → adiciona à lista.
- [ ] Criar regra de prioridade maior: pattern "uber trip" + Mercado + prioridade 10.
- [ ] Click "Editar" preenche o form; mudar prioridade pra 5 + Salvar.
- [ ] Click "Apagar" prompt confirm; cancelar não apaga, OK apaga.
- [ ] Botão "Aplicar a existentes" → mostra "N transações categorizadas".
- [ ] Importar um OFX novo (precisa de arquivo real) → InsertResult.auto_categorized > 0 quando há matches; verifique no console (`F12` ou `console.log` no Import).
- [ ] Em `/transactions`, transações novas vêm com categoria já preenchida quando há regra que casa.
- [ ] Transação já categorizada manualmente NÃO muda ao "Aplicar a existentes".

```bash
pkill -f "tauri dev"; pkill -f "vite"; pkill -f "target/debug/finan"
```

- [ ] **Step 3: Update README**

```
## Status

- ✅ Fase 0 — Scaffold (Tauri + Svelte + DB + sidebar + IPC tipado)
- ✅ Fase 1 — Importar OFX (parser TS + dedup por FITID + listagem)
- ✅ Fase 2 — Categorização manual inline + filtros + notes
- ✅ Fase 3 — Regras automáticas (description-contains + apply-existing)
- 🚧 Fase 4 — Dashboard (KPIs + donut + barras) (próximo)
- ⏳ Fase 5 — Polish (search, settings, atalhos)
```

- [ ] **Step 4: Closing commit**

```bash
git add README.md
git commit -m "$(cat <<'EOF'
chore(fase-3): close rules phase — acceptance criteria batem

- Migration 0002 cria tabela rules + index priority/created_at
- 5 commands Rust: list_rules, create_rule, update_rule, delete_rule,
  apply_rules_to_uncategorized
- apply_rules_internal: única UPDATE SQL com correlated subquery (first match wins)
- Auto-apply integrado em insert_transactions; InsertResult.auto_categorized
- UI /rules: RuleForm (criar/editar) + RulesList + botão "Aplicar a existentes"
- Sidebar adiciona link "Regras"
- Tests: 27 cargo (21 anteriores + 5 rules + 1 migrations) / 19 vitest / 0 erros
- Build: ~165 KB JS

Próximo: plano da Fase 4 (Dashboard com LayerChart).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Self-Review

### Spec coverage

| Spec item | Task |
|---|---|
| §5 migration 0002 (rules table) | T1 |
| §8.6 CRUD de regras + lista | T2, T5, T6 |
| §8.6 "Aplicar a transações existentes" | T2 (command), T6 (UI), §8.6 "só sem categoria" | T2 |
| §8.6 "vence a de maior priority; empate created_at" | T2 (apply_rules_internal SQL ORDER BY) |
| §8.6 "Manual nunca sobrescreve" | T2 (`WHERE category_id IS NULL`) + test |
| §9 Fase 3 critério "aplicação automática no import" | T3 (auto-call no insert_transactions) |
| §9 Fase 3 critério "criar regra uber → Transporte, ver no próximo import" | T3 + T6 + manual test em T8 |
| §10 cargo tests aumentam | T1 (1), T2 (5) = 6 novos |

### Placeholder scan

Reli todos os blocos. Nenhum "TBD" / "TODO" / "implement later" / "add error handling". Cada step tem código completo.

### Type consistency

- `Rule { id, pattern, category_id, priority, created_at }` — Rust domain (T1) → bindings (T4) → RulesList (T6) → /rules (T6). Match.
- `NewRule` / `UpdateRule` — mesmas 3 propriedades: pattern, category_id, priority. Match.
- `InsertResult { inserted, skipped_duplicates, auto_categorized }` — Rust (T3 extends) → bindings (T4 regenerates) → existing TS callers in Fase 1 Import.svelte still work (destructuring `result.inserted` etc. é não-exaustivo).
- `applyRulesToUncategorized(accountId: number | null)` — Rust signature `Option<i64>` → TS `number | null`. Match.
- `apply_rules_internal` é pub na crate, chamada por `commands::transactions::insert_transactions` via `crate::commands::rules::apply_rules_internal`. Path consistent.

### Risks documented inline

- **T3 step 2:** `&mut *conn` necessário pra desreferenciar o `MutexGuard`. Plano explicita.
- **T4 break:** `pnpm check` deve estar 0 erros após T4 — `InsertResult` ganha campo opcional que TS aceita silenciosamente (destructuring não falha por campo extra).
- **T6 confirm() nativo:** UX inferior a modal custom, mas zero deps adicionais e suficiente pra MVP. Refatorar pra Dialog em fase 5 polish.
- **`apply_rules_internal` correlated subquery:** SQLite executa duas vezes por linha (uma no `EXISTS`, outra no `SET`). Performance OK pra <10k tx; se virar gargalo, refatorar pra CTE + JOIN em fase futura.
